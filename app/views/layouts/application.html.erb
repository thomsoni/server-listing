<!DOCTYPE html>
<html>
<head>
  <title>Server Listing</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="nav_header">
    <div class="nav_header h_image">
      <%= link_to image_tag("Craig.png"), root_url %>
    </div>
    <div class="nav_header h_text">
      <h1 style="nav_header h1">Casino Rama Servers</h1>

	  <b>All Production and Test Servers</b>

    </div>
</div>


<div class="container">

<%= yield %>

</div>

<div class="nav_footer">
 
  <% if user_signed_in? %>
    Logged in as <b> <%= current_user.username %> (<%= current_user.email %>) </b> | <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
  <% else %>
    <%= link_to "Login", login_path %>
  <% end %>  

</div>

</body>
</html>
